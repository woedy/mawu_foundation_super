[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --include=dev"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "NODE_ENV=production npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
