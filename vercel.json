{
  "buildCommand": "npm install --prefer-offline && cd apps/web && npm rebuild && npm run build",
  "installCommand": "npm install --prefer-offline",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "regions": ["iad1"]
}
