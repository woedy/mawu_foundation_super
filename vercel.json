{
  "installCommand": "npm install",
  "buildCommand": "ROLLUP_USE_WASM=true npm run build --workspace @mawu/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "regions": ["iad1"]
}
