# Production Environment Configuration Template
NODE_ENV=production

# Server Configuration
PORT=3000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/mawu_foundation_prod

# Stripe (Production Keys - Replace with actual values)
STRIPE_SECRET_KEY=sk_live_your_production_stripe_secret_key
VITE_STRIPE_PUBLIC_KEY=pk_live_your_production_stripe_public_key
# Get webhook secret from: https://dashboard.stripe.com/webhooks (LIVE MODE)
# After creating webhook endpoint at: https://your-domain.com/api/webhooks/stripe
# IMPORTANT: Use LIVE mode webhook secret for production
STRIPE_WEBHOOK_SECRET=whsec_your_production_webhook_secret

# Email Service (Gmail SMTP - Production)
EMAIL_USER=foundation@mawufoundation.org
EMAIL_PASS=your-production-app-specific-password

# Admin Configuration
ADMIN_EMAIL=admin@mawufoundation.org
ADMIN_PASSWORD=secure_production_password
ADMIN_NAME=Foundation Admin

# Session Security (Generate a secure random string)
SESSION_SECRET=your-secure-production-session-secret-key

# API Configuration
# VITE_API_URL - Base URL for backend API (defaults to http://localhost:3001 if not set)
# Production: Use your production API domain with HTTPS
# Example: https://api.mawufoundation.org or https://your-domain.com/api
VITE_API_URL=https://api.mawufoundation.org
FRONTEND_URL=https://mawufoundation.org

# Analytics (Optional)
# VITE_ANALYTICS_DOMAIN=mawufoundation.org
